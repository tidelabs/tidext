window.SIDEBAR_ITEMS = {"struct":[["KeyProvider","The [`KeyProvider`] keeps secrets in [`NCKey`] at rest, such that no key can be directly read out from memory. The memory fragments of the key provider will be rotated continuously while not in use."],["KeyStore","The [`KeyStore`] keeps a map of [`VaultId`] -> [Vec] representing encrypted [`Key<P>`] using the `master_key`. `master_key` is stored in a non-contiguous data structure [`NCKey<P>`] for more security"]]};