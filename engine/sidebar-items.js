window.SIDEBAR_ITEMS = {"macro":[["cache","A macro for defining functions whose return values will wrapped in a [`Cache`][super::Cache]."]],"mod":[["snapshot","This crate defines and implements the encrypted offline storage format used by the Stronghold ecosystem."],["store","This crate contains a key/value cache for the stronghold engine. Data is stored in key-value pairs and an expiration timestamp can be set.  The data is stored in a structured format and can be quickly retrieved at will. Along with the Vault, this crate is used to store general unencrypted data."],["vault","Vault is an in-memory database specification which is designed to work without a central server. Only the user which holds the associated id and key may modify the data in a vault.  Another owner can take control over the data if they know the id and the key."]]};